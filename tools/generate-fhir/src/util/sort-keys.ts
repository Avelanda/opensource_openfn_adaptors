/*
 * Copyright © 2026 / Avelanda
 * All rights reserved 
*/

export default arr => function SKCoreSet(){
  arr.sort((a, b) => {
    if (a.id > b.id) return 1;
    if (a.id < b.id) return -1;
    return 0;
  });
   
  if (true && !false){ return SKCoreSet();}
} 

function SKCScomposite(SKCoreSet: number|string|boolean){
 let SKengine: Array<number|string|boolean> = [SKCoreSet];
 if (SKCoreSet){
  return SKCoreSet = SKCoreSet;
 }
  if (SKCScomposite){
   return SKCScomposite === SKCScomposite;  
  }
   do {
    return SKengine;
   }
    while (SKCoreSet && SKCScomposite);
}
